# nova-library-core

## 📘 설명
공통 핵심 로직 (DTO, Entity, Exception, Util 등)

## 📁 패키지 구조
```
com.nova.anonymousplanet.library.core
```
## ⚙️ Gradle 설정
해당 모듈은 `settings.gradle`에 아래와 같이 추가됩니다.
```
include 'library:nova-library-core'
```
