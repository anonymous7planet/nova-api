# nova-library-persistence

## 📘 설명
JPA, QueryDSL, Auditing 설정

## 📁 패키지 구조
```
com.nova.anonymousplanet.library.persistence
```
## ⚙️ Gradle 설정
해당 모듈은 `settings.gradle`에 아래와 같이 추가됩니다.
```
include 'library:nova-library-persistence'
```
