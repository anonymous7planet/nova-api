plugins {
    id 'java'
}

java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}

def queryDslVersion = "7.0"
def querydslMainDir = layout.buildDirectory.dir("generated/querydsl/main")
def querydslTestDir = layout.buildDirectory.dir("generated/querydsl/test")

plugins.withId("java-library") {

    tasks.withType(JavaCompile).configureEach {
        if (name.contains("Test")) {
            generatedSourceOutputDirectory = querydslTestDir
        } else {
            generatedSourceOutputDirectory = querydslMainDir
        }
    }
}
// 핵심: clean 시 생성된 폴더도 지우도록 설정
clean {
    delete querydslMainDir
    delete querydslTestDir
}

dependencies {
    // Spring Data JPA
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

    implementation 'jakarta.persistence:jakarta.persistence-api:3.1.0'

    // MySQL connector
    runtimeOnly 'com.mysql:mysql-connector-j'

    // QueryDSL
    implementation("io.github.openfeign.querydsl:querydsl-core:${queryDslVersion}")
    implementation("io.github.openfeign.querydsl:querydsl-jpa:$queryDslVersion")
    annotationProcessor("io.github.openfeign.querydsl:querydsl-apt:$queryDslVersion:jpa")

    // ObjectMapper 사용을 위한 최소 의존성
    implementation 'com.fasterxml.jackson.core:jackson-databind'
    // Java 8 날짜/시간 모듈 (이게 있어야 JavaTimeModule 사용 가능)
    implementation 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310'

    // core의 인터페이스(BaseEnum 등)가 필요하지만 외부로 노출하지 않음
    implementation project(':library:nova-core')

    annotationProcessor "jakarta.annotation:jakarta.annotation-api"
    annotationProcessor "jakarta.persistence:jakarta.persistence-api"
}


//sourceSets {
//    main.java.srcDirs += querydslDir
//}



///**
// * [Project Nova] QueryDsl 빌드 설정
// * Main/Test 소스셋 분리 및 Jakarta Persistence 대응
// */
//def querydslMainDir = layout.buildDirectory.dir("generated/querydsl/main")
//def querydslTestDir = layout.buildDirectory.dir("generated/querydsl/test")
//
//dependencies {
//    // Jakarta 대응 버전 명시
//    api 'com.querydsl:querydsl-jpa:5.1.0:jakarta'
//    annotationProcessor "com.querydsl:querydsl-apt:5.1.0:jakarta"
//    annotationProcessor "jakarta.persistence:jakarta.persistence-api"
//    annotationProcessor "jakarta.annotation:jakarta.annotation-api"
//}
//
//sourceSets {
//    main {
//        java {
//            srcDirs += [ querydslMainDir ]
//        }
//    }
//    test {
//        java {
//            srcDirs += [ querydslTestDir ]
//        }
//    }
//}
//
//tasks.withType(JavaCompile).configureEach {
//    // Java 21의 파라미터 이름 보존 설정 (QueryDsl 매핑에 도움)
//    options.compilerArgs << "-parameters"
//
//    if (name.contains("Test")) {
//        options.generatedSourceOutputDirectory.set(querydslTestDir)
//    } else {
//        options.generatedSourceOutputDirectory.set(querydslMainDir)
//    }
//}
//
//clean {
//    delete querydslMainDir
//    delete querydslTestDir
//}