<?xml version="1.0" encoding="UTF-8"?>
<included>
    <property name="ERROR_LOG_PATTERN"
                value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [${SERVICE_NAME}] [%thread] %logger{36} - %msg%replace( [traceId=%X{traceId}]){' \[traceId=\]', ''}%replace( [requestId=%X{requestId}]){' \[requestId=\]', ''}%replace( [clientIp=%X{clientIp}]){' \[clientIp=\]', ''}%replace( [userId=%X{userId}]){' \[userId=\]', ''}%replace( [userUuid=%X{userUuid}]){' \[userUuid=\]', ''}%replace( [userRole=%X{userRole}]){' \[userRole=\]', ''}%n%fullException"/>

    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_BASE_DIR}/${SERVICE_NAME}/${SERVICE_NAME}-error.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_BASE_DIR}/${SERVICE_NAME}/errors/${SERVICE_NAME}-error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${ERROR_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="ASYNC_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ERROR_FILE" />
        <queueSize>1024</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>true</includeCallerData>
    </appender>

</included>