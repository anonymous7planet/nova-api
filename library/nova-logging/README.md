# nova-library-logging

## 📘 설명
공통 로깅 시스템 (Logback, MDC 설정 포함)

## 📁 패키지 구조
```
com.nova.anonymousplanet.library.logging
```
## ⚙️ Gradle 설정
해당 모듈은 `settings.gradle`에 아래와 같이 추가됩니다.
```
include 'library:nova-library-logging'
```
